services:
  - type: web
    name: yt-audio-downloader
    env: docker
    plan: free
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production
      - key: YT_DLP_PATH
        value: /usr/local/bin/yt-dlp
      - key: CORS_ORIGIN
        value: "*"
